# Name the command that find, in real time, currently running processes
top

# top program prvodies a dynamic real-time view of a running system.
# ps utility prints the process status to output. The following options give the required details:
# a: select all processes (others and your own)
# u: all processes on a terminal
# x: show processes includinng those that don't have a controlling terminal (ttys).
